<Window x:Class="OnlineTests.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DOTA2SCHOOL" Height="440" Width="440"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D44"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#7B1FA2"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="#2D2D44"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#7B1FA2"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#7B1FA2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinHeight" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="15,10">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            TextElement.FontWeight="Bold"
                                            TextElement.FontSize="15"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#9C27B0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="#1E1E2F">
        <Border Background="#2D2D44" 
                CornerRadius="10" 
                Width="340" 
                Padding="35,30"
                HorizontalAlignment="Center" 
                VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="DOTA2SCHOOL" 
                           Foreground="White" 
                           FontSize="22" 
                           FontWeight="Bold" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,30"/>

                <TextBlock Text="Логин" Foreground="#AAAAAA" FontSize="12" Margin="0,0,0,5"/>
                <TextBox x:Name="LoginTextBox" Height="38" Margin="0,0,0,15"/>

                <TextBlock Text="Пароль" Foreground="#AAAAAA" FontSize="12" Margin="0,0,0,5"/>
                <PasswordBox x:Name="PasswordBox" Height="38" Margin="0,0,0,25"/>

                <Button Content="ВОЙТИ" Click="LoginButton_Click"/>

                <TextBlock x:Name="ErrorText" 
                           Foreground="#FF6B6B" 
                           HorizontalAlignment="Center" 
                           Margin="0,15,0,0" 
                           FontSize="12" 
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>