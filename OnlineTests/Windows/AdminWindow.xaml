<Window x:Class="OnlineTests.Windows.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Администратор | Online Tests" Height="650" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="#AAAAAA"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4,4,0,0">
                            <ContentPresenter Content="{TemplateBinding Header}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#7B1FA2"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3D3D5C"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ListView">
            <Setter Property="Background" Value="#2D2D44"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style TargetType="ListViewItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5,3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}">
                            <GridViewRowPresenter HorizontalAlignment="Stretch" 
                                                  VerticalAlignment="Center"
                                                  Content="{TemplateBinding Content}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3D3D5C"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#7B1FA2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#2D2D44"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
        </Style>
    </Window.Resources>

    <Grid Background="#1E1E2F">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2D2D44" Padding="12">
            <Grid>
                <TextBlock x:Name="WelcomeText" Foreground="White" FontSize="16" 
                           VerticalAlignment="Center" HorizontalAlignment="Left"/>
                <Button Content="ВЫЙТИ" Background="#7B1FA2" HorizontalAlignment="Right"
                        Click="LogoutButton_Click"/>
            </Grid>
        </Border>

        <TabControl Grid.Row="1" Background="#1E1E2F" Margin="10" 
                    SelectionChanged="TabControl_SelectionChanged">
            <TabItem x:Name="StudentsTab" Header="СТУДЕНТЫ">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <ListView x:Name="StudentsListView" Margin="5">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="40"/>
                                <GridViewColumn Header="ФИО" DisplayMemberBinding="{Binding FullName}" Width="200"/>
                                <GridViewColumn Header="Логин" DisplayMemberBinding="{Binding Login}" Width="120"/>
                                <GridViewColumn Header="Телефон" DisplayMemberBinding="{Binding Phone}" Width="130"/>
                                <GridViewColumn Header="Дата рождения" DisplayMemberBinding="{Binding BirthDate}" Width="120"/>
                                <GridViewColumn Header="" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="УДАЛИТЬ" Background="#FF6B6B"
                                                    Click="DeleteStudent_Click" Tag="{Binding Id}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                    <Button Grid.Row="1" Content="ОБНОВИТЬ" Background="#4CAF50" 
                            HorizontalAlignment="Left" Margin="5" Click="RefreshStudentsButton_Click"/>
                </Grid>
            </TabItem>
            <TabItem x:Name="TestsTab" Header="ТЕСТЫ">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <ListView x:Name="TestsListView" Margin="5">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="40"/>
                                <GridViewColumn Header="Название" DisplayMemberBinding="{Binding Title}" Width="180"/>
                                <GridViewColumn Header="Описание" DisplayMemberBinding="{Binding Description}" Width="250"/>
                                <GridViewColumn Header="Балл" DisplayMemberBinding="{Binding MaxScore}" Width="60"/>
                                <GridViewColumn Header="Вопросов" DisplayMemberBinding="{Binding QuestionCount}" Width="70"/>
                                <GridViewColumn Header="" Width="170">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Content="✏️" Background="#4CAF50" Width="35"
                                                        Click="EditTest_Click" Tag="{Binding Id}"/>
                                                <Button Content="🗑️" Background="#FF6B6B" Width="35"
                                                        Click="DeleteTest_Click" Tag="{Binding Id}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="5">
                        <Button Content="ДОБАВИТЬ ТЕСТ" Background="#7B1FA2" Click="AddTestButton_Click"/>
                        <Button Content="ОБНОВИТЬ" Background="#4CAF50" Margin="8,0,0,0" Click="RefreshTestsButton_Click"/>
                    </StackPanel>
                </Grid>
            </TabItem>
            <TabItem x:Name="ResultsTab" Header="РЕЗУЛЬТАТЫ">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBlock Text="Сортировка:" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <ComboBox x:Name="SortComboBox" Width="130" Height="25" 
                                  SelectionChanged="SortComboBox_SelectionChanged">
                            <ComboBoxItem Content="По дате" Tag="date" IsSelected="True"/>
                            <ComboBoxItem Content="По студенту" Tag="student"/>
                            <ComboBoxItem Content="По тесту" Tag="test"/>
                            <ComboBoxItem Content="По баллам" Tag="score"/>
                        </ComboBox>
                        <Button Content="ЭКСПОРТ В CSV" Background="#4CAF50" Margin="15,0,0,0"
                                Click="ExportButton_Click"/>
                        <Button Content="ОБНОВИТЬ" Background="#4CAF50" Margin="8,0,0,0"
                                Click="RefreshResultsButton_Click"/>
                    </StackPanel>

                    <ListView x:Name="ResultsListView" Grid.Row="1" Margin="5">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Студент" DisplayMemberBinding="{Binding StudentName}" Width="200"/>
                                <GridViewColumn Header="Тест" DisplayMemberBinding="{Binding TestTitle}" Width="250"/>
                                <GridViewColumn Header="Дата" DisplayMemberBinding="{Binding AttemptDate}" Width="150"/>
                                <GridViewColumn Header="Баллы" DisplayMemberBinding="{Binding Score}" Width="80"/>
                                <GridViewColumn Header="Процент" DisplayMemberBinding="{Binding PercentCorrect}" Width="80"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </TabItem>

        </TabControl>
    </Grid>
</Window>